{
  "name": "eminded-qa-pipeline",
  "version": "2.0.0",
  "description": "Pipeline QA automatizado: Call Tools → Deepgram → Claude (Router + Prompts) → GHL",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@deepgram/sdk": "^3.9.0",
    "axios": "^1.7.0",
    "dotenv": "^16.4.0",
    "express": "^4.19.0",
    "multer": "^2.1.1"
  }
}
